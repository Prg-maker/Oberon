import {StyleSheet} from 'react-native'
import {theme} from '../../theme'


export const styles = StyleSheet.create({
  container:{
    width: 201,
    height: 224,
    backgroundColor: '#fff',
    borderRadius: 10,
    alignItems:'center',
    justifyContent: 'center'
  },


  plus:{
    backgroundColor: theme.colors.colorBckGray,
    borderRadius: 50,
    padding: 25,
    
  }
})

